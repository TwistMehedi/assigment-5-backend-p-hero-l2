enum UserStatus {
    ACTIVE
    BLOCKED
    SUSPEND
}

enum Role {
    USER
    ADMIN
    CREATOR
}

enum SubscriptionStatus {
    FREE
    MONTHLY
    YEARLY
}

enum ReviewStatus {
    PENDING
    PUBLISHED
}

enum ContentType {
    MOVIE
    SERIES
}

enum MediaStatus {
    PENDING
    APPROVED
    REJECTED
}

enum PurchaseType {
    MOVIE
    SERIES
}

enum PurchaseStatus {
    PENDING
    COMPLETED
    FAILED
    REFUNDED
}
