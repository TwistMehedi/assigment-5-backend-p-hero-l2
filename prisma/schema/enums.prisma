enum UserStatus {
    ACTIVE
    BLOCKED
    SUSPEND
}

enum Role {
    USER
    ADMIN
    DIRECTOR
}

enum SubscriptionStatus {
    FREE
    MONTHLY
    YEARLY
}

enum ReviewStatus {
    PENDING
    PUBLISHED
}

enum ContentType {
    MOVIE
    SERIES
}
