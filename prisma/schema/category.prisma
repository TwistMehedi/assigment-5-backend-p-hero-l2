model Categories {
    id         String  @id @default(uuid())
    name       String  @unique
    mediaCount String?
    slug       String?
    userId     String

    user User @relation(fields: [userId], references: [id])
}
